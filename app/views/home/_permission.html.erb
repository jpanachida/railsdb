<tr<%= alt %>>
  <td><%= h permission.name %></td>
  <td><%= h permission.description %></td>
  <td><%= link_to 'Edit', :controller => 'home', :action => 'edit_permission', :id => permission.id %></td>
  <td><%= link_to 'Del', :controller => 'home', :action => 'del_permission', :id => permission.id %></td>
</tr>
